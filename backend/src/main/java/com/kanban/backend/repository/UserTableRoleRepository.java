package com.kanban.backend.repository;

import com.kanban.backend.model.UserTableRole;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserTableRoleRepository extends JpaRepository<UserTableRole, Long> {
}
